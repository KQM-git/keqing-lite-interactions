embeds:
  - title: Keqing Help - Calculators
    color: ${ COLORS.KQM_PURPLE }
    thumbnail:
      url: ${ IMAGES.KQM_LOGO }
    description: |-
      `${ TRIGGER_PREFIX }calc [equation]`: Simple calculator when you need to do math.
      `${ TRIGGER_PREFIX }fischlcalculator`:
      `${ TRIGGER_PREFIX }gcc` / `${ TRIGGER_PREFIX }keqcalc`: Keqing Gear Comparison Calculator
      `${ TRIGGER_PREFIX }gcsim`: The gcsim
      `${ TRIGGER_PREFIX }optimizer / ${ TRIGGER_PREFIX }optimiser`: The Genshin optimizer
      `${ TRIGGER_PREFIX }wolfram`: Wolfram Alpha, for when the simple calculator fails.
      
buttons:
  - customId: "liveInteraction#help/guides"
    label: Back - Guide Commands
    style: "PRIMARY"
    emoji: "◀️"

  - customId: "liveInteraction#help/links"
    label: Next - Link Commands
    style: "PRIMARY"
    emoji: "▶️"